The final output of dictionary is saved in the file "assignment1_dictionary.txt".

The index function is written without lambda function, and the index_lambda function is written with the lambda function.

The test query is "Study time is tight."

